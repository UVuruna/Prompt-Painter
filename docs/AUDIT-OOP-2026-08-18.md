# AUDIT — One Kind, One Class + Structure (2026-08-18)

Owner-ordered probe (`rules/history/one-kind-one-class.md`) for PromptPainter,
verified/deepened on the CODE (grep+read), not on impression. Source scope:
`gui/`, `painter/`, `main.py` — tests excluded except where the STRUCTURE
RATCHET names a test file. Commit at time of audit: `a0c1f37`.

## Step 0 — the 1280x720 floor is now information

Owner decree 2026-08-18 abolished the fixed screen floor. Only
`tests/test_layout_audit_tk.py` enforced it (grep across `tests/`, `docs/`,
`*.md`, `.claude/` found no other guard); `tests/test_layout_law.py` and
`tests/layout_checks_tk.py` never referenced 1280/720. Changed:

- `tests/test_layout_audit_tk.py:82-88` — `FLOOR_WIDTH, FLOOR_HEIGHT = 1280, 720`
  → `PROFILE_SCREEN_WIDTH, PROFILE_SCREEN_HEIGHT = 1280, 720` (kept as the
  smallest mandatory profile screen, `rules/devices.json` `pc-low` — same
  numbers, different meaning: reporting reference, not a verdict).
- `run_dialog_audit` (was `tests/test_layout_audit_tk.py:530-533`) — the
  `problems.append("ABSURD MINIMUM …")` branch replaced with a `print(...)`
  report per dialog window; CLIPPED/ESCAPES checks in the same function are
  untouched.
- `run_audit` (was `tests/test_layout_audit_tk.py:608-618`) — same change for
  `PainterGui`'s own minsize.
- Docstring at `tests/test_layout_audit_tk.py:11-13,35-38` reworded off "fits
  THE SCREEN FLOOR 1280x720" to the informational framing.
- No hits in `docs/DECISIONS.md`, `CLAUDE.md`, `instructions.md`,
  `PROTOCOL.html`, `tests/___tests.md` — nothing else to change.

`!` `python tests/run_guards.py` → `18 passed`, exit `0`, ~42s (both before
and after — the CLIPPED/ESCAPES checks are unchanged, only the floor verdict
was removed). `!` commit `a0c1f37` "0.0.290 The screen floor was information
all along". `!` `git push origin main` → `485264e..a0c1f37 main -> main`.

## Step 1.1 — Kind map

| Kind | Defined | Instances | New instance today |
|---|---|---|---|
| Tool-settings panel | `ToolSettingsPanel` — `gui/tool_panels/base.py:55` | 5: `BgSettingsPanel` (bg.py:52), `CropSettingsPanel`/`UpscaleSettingsPanel`/`AspectSettingsPanel` (geometry.py:55/138/207), `ImageCheckerSettingsPanel` (image_checker.py:24) | **subclass** — good |
| Dashboard job panel | `JobPanel` — `gui/dash_panels.py:70` | 3: `DashPanel` (dash_panels.py:257), `ToolPanel` (tool_dash.py:60), `AiCheckPanel` (tool_dash.py:341) | **subclass** — good |
| Main-window responsibility slice | 6 mixins composed in `gui/app.py:28` | `BuildMixin`, `ViewMixin`, `SiteJobsMixin`, `CheckerFixerMixin`, `ToolJobsMixin`, `SettingsMixin` — one class, `PainterGui`, MRO-glued | **new mixin, composed** — good |
| Top-level window (`tk.Toplevel`) | no shared base beyond `tk.Toplevel`/`_ModalToolDialog` | 6: `DocWindow`, `ImageViewer`, `BeforeAfterWindow`, `StepRestoreWindow`, `SelectWindow`, `AiKeyWizard` | **COPIED BLOCK** — VIOLATION, see below |
| Background-probe poll loop (queue + `self.after`) | no shared base/helper | 5 methods across 4 classes: `ApiImageGenPanel._poll_probe`/`_poll_models`, `_AiDialog._poll`, `ModelPickerRow._poll`, `SheetGenPanel._poll` (+ paired `_refresh*`/`_apply*result`) | **COPIED BLOCK** — VIOLATION, see below |
| Typed aspect W/H field handler | no shared base/helper | 3: `AgentPanel._on_force_aspect_wh_typed` (agent_panel.py:723), `ApiImageGenPanel._on_force_aspect_wh_typed` (api_panel.py:471), `AspectSettingsPanel._on_wh_typed` (tool_panels/geometry.py:281) | **COPIED BLOCK** — VIOLATION, see below |
| Image generator (site) | `SITE_FILE_SUFFIX` table — `painter/config/paths.py` | 3: `chatgpt`, `gemini`, `api_image` | **registry entry** — good |
| Postprocess step | 3 independent functions (`remove_background`/`crop_transparent`/`upscale_if_small`) | 3, each a genuinely different algorithm, not variants of one kind | not a "kind" — below the bar |

### VIOLATION 1 — Toplevel setup ritual, copied in 5 constructors

The literal `title / minsize / skin_toplevel / THEME_TOPLEVELS.append` block:

| File:lines | Class |
|---|---|
| `gui/doc_window.py:65-68` | `DocWindow` |
| `gui/restore_windows.py:68-71` | `BeforeAfterWindow` |
| `gui/restore_windows.py:283-286` | `StepRestoreWindow` |
| `gui/select_window.py:96-99` | `SelectWindow` |
| `gui/image_viewer.py:126-128` | `ImageViewer` (no `title()` line — sets a `StringVar` later — otherwise identical) |

`gui/dialogs.py:98-102` (`_ModalToolDialog`, base of `AiKeyWizard`) does the
`skin_toplevel` half but not `THEME_TOPLEVELS.append` (fixed-size dialogs
don't theme-flip the same way) — a near-miss, not counted as a 6th instance.

### VIOLATION 2 — poll-loop family (clone_guard-confirmed, see Step 1.2)

**REVERSED — owner accepted, R3 DONE 0.0.295: the three groups are
gone and `tests/clone_ratchet.json` is now empty.**
`gui/__about/api_panel.md` documented this as deliberate: *"duplicated rather
than shared via a mixin since this panel's base class differs"* (`ttk.Frame`
vs. `tk.Toplevel`-hosted `_AiDialog`). That reasoning is real but the escape
mechanism used is the **ratchet** (`tests/clone_ratchet.json`, meant to
SHRINK, never a permanent allowance) rather than an inline `clone-ok:`
reason — the two mean different things and the wrong one is in use here (see
Grade below).

### VIOLATION 3 — aspect W/H typed handler (new finding, not in the ratchet)

3-way clone at 7 statements each — one under the guard's default
`--min-statements 8`, so `run_guards` FULL never sees it. Confirmed with
`--min-statements 6` (Step 1.2). Same shape everywhere: read both entries,
parse ints, guard against 0, call the aspect-apply callback.

## Step 1.2 — Clone list (`rules/tools/clone_guard.py`)

Default (`--min-statements 8`, matches the guard in `run_guards.py` FULL)
against `tests/clone_ratchet.json`: **0 failing, 0 stale** — the 3 ratcheted
groups are exactly the poll-loop family above.

`--min-statements 6` (no ratchet, audit-only, threshold from the brief) on
the whole project: **4 cross-file groups** — the 3 ratcheted ones plus the
new `_on_force_aspect_wh_typed`/`_on_wh_typed` 3-way clone (Violation 3).

`--min-statements 4` restricted to `gui/`: same 4 groups plus one nested
closure (`ApiImageGenPanel._refresh_models.work` / `ModelPickerRow._refresh.
work`, a sub-clone already inside the ratcheted `_refresh_models`/`_refresh`
pair — not a new group) and 2 same-file WARNs (never fail):
`image_viewer.py:396`/`457` (`_toggle_check`/`_toggle_steps`) and
`widgets.py:99`/`110` (`tk_font`/`ctk_font`).

| Group | Members | Absorbing abstraction |
|---|---|---|
| Toplevel setup | 5 constructors (Violation 1 table) | a small helper, e.g. `theme.finish_toplevel(win, *, title=None, minsize=None)` called from each `__init__` |
| Poll loop | `api_panel.py:551,606`; `dialogs.py:73`; `model_picker.py:100`; `sheetgen_panel.py:220` | a free function `poll_worker_queue(widget, q, on_result, *, poll_ms, after_attr)` — sidesteps the "base class differs" objection because it needs no shared base |
| Refresh-models | `api_panel.py:577`; `model_picker.py:80` | same helper family, or a small `ModelDiscovery` value object both classes call |
| Apply-models-result | `api_panel.py:617`; `model_picker.py:111` | same |
| Aspect W/H typed | `agent_panel.py:723`; `api_panel.py:471`; `tool_panels/geometry.py:281` | a helper `apply_typed_wh(entry_w, entry_h, on_apply)` |

## Step 1.3 — God-files (source, not tests)

`MAX_LINES = 1000` (`tests/test_structure_law.py:35`). Every file > 800 lines:

| File | Lines | Ratcheted | Responsibilities (2-5) |
|---|---|---|---|
| `painter/driver.py` | 1599 | yes | Chrome attach/lifecycle · composer protocol (type/send/confirm) · turn-based done-edge + anchoring · recovery ladder (retry/refresh/new_chat) · refusal/degrade classification |
| `gui/app_jobs.py` | 1110 | yes | site run loop (`_start_site`/`_drive_site`/`_stop_site`/cooldown/restart) · the API-image job (`_start_api_image`) · dashboard event dispatch (`_drain_queue`/`_dispatch`) |
| `gui/agent_panel.py` | 1051 | yes | one site's control surface + 5 UI-sketch sub-panel builders (`_build_bg_sub`/`_build_aspect_sub`/`_build_upscale_sub`/`_build_checker_sub`/`_build_pacing_sub`, lines 537-720) |
| `gui/app_build.py` | 995 | no | `PainterGui` constructor · widget-construction helpers · font-zoom/wheel routing · drag-resize watcher |
| `painter/runner.py` | 979 | no | run loop: queue → done-edge → save → fix → report → resume → pace |
| `gui/dash_panels.py` | 972 | no | `JobPanel` base + `DashPanel` (site live view: progress/stats/history/restore/checker report) |
| `gui/app_settings.py` | 971 | no | queue mgmt · sheet parsing/planning · dashboard row viewers · prerequisite buttons · AI key gate · settings round-trip |
| `gui/widgets.py` | 909 | no | themed widget toolkit (buttons/entries/combos/switches/`FlowRow`/`Accordion`) + numeric field parsers |
| `gui/api_panel.py` | 883 | no | `ApiImageGenPanel` (paid API job settings panel) + `ApiImageAdapter` |

Test coverage / risk if split:
- `painter/driver.py` — `tests/test_driver.py` (1853 lines, ratcheted
  alongside it) → LOW risk, dedicated heavy coverage.
- `gui/app_jobs.py` — no dedicated test file; exercised only INDIRECTLY
  through `test_gui_pipeline.py`, `test_gui_running_view.py`,
  `test_gui_degrade.py` and others via `PainterGui` integration → MEDIUM
  risk.
- `gui/agent_panel.py` — `tests/test_gui_agent_visibility.py` (496 lines) +
  referenced by 6 more `test_gui_*.py` files → LOW-MEDIUM risk.
- The 6 files under the wall (880-999 lines) are not violations; `runner.py`
  has strong dedicated coverage (`test_runner_*.py`, 4 files, ~2100 lines
  total) if ever split; the rest have only integration coverage.

## Step 1.4 — Config-in-code

Almost nothing found — `painter/config/*.py` already carries the registries
(`SITE_FILE_SUFFIX`, `JOB_LABEL`/`JOB_ICON_PX`, `BG_MODE_LABEL`,
`RETRY_PREAMBLES`, theme tables). One minor candidate:

- `gui/app_build.py:883` — `("#4338ca", "#818cf8")`, a literal button-colour
  pair, the only hex-colour literal outside `gui/theme.py` that is not a
  computed black/white text-contrast default (`agent_panel.py:710,928` pick
  `#000000`/`#ffffff` by luma — a two-value algorithm, not a config table).
  Low value, trivial to move into `gui/theme.py`.

## Step 1.5 — Layer violations

None found. `painter/` never imports `gui` (`grep` empty); the 4 `painter/`
files that mention "tkinter" (`filters.py:8`, `config/aspect.py:39`,
`config/jobs.py:19,194`, `config/theme.py:5-23`) do so only in COMMENTS
stating the opposite — the engine deliberately stays tkinter-free so tests
and non-GUI code can import `painter.config` without pulling in Tk. `gui/`
never touches Playwright/CDP directly (`app_jobs.py:805` is a comment, not an
import) and no `gui/*.py` does raw `shutil`/`os.remove`/direct file writes
outside settings persistence.

## Step 1.6 — Tests-changed-to-pass smell

`git log -p --since=60.days -- tests` (132 commits touching tests): no
`@pytest.mark.skip`/`xfail`/`pytest.skip()` additions. Two
`@pytest.mark.skipif` exist, both legitimate environment gates for
integration tests (`tests/test_postprocess.py:778` — skips only when the
real `OldAge.png` fixture is absent; `tests/test_upscale.py:175` — skips
only when the real `realesrgan-ncnn-vulkan` binary was never downloaded).
No suspicious weakening found.

## Step 1.7 — Grades

**Law 8 (One Kind, One Class): 8/10.**
1. The two real ≥3-instance kinds are correctly modeled — `ToolSettingsPanel`
   (5 subclasses) and `JobPanel` (3 subclasses) — new instances are
   subclasses, never copies.
2. Exactly one true copied-block violation is CONFIRMED at file:line across
   5 `Toplevel` constructors (Violation 1) — small, mechanical, easy fix.
3. `clone_guard --min-statements 6` surfaces a 4th cross-file clone
   (Violation 3, aspect W/H typed handler) sitting ONE statement under the
   guard's own default threshold — real duplication currently invisible to
   `run_guards` FULL.

**Structure: 6/10.**
1. All 3 files over the 1000-line wall are RATCHETED with an owner-approved
   split plan already WRITTEN into the ratchet text (`tests/
   test_structure_law.py:56-81`) — not a fresh discovery.
2. That "second round" was approved 2026-07-30 and has not been executed
   ~19 days later — the debt named in the ratchet is old, not new.
3. 6 more files now sit in the 880-999 line band (none yet a violation) —
   the same pattern that produced the first 3 god-files is forming again
   underneath the ratchet.

## Step 2 — Refactor plan

| # | What | Why | Files | Mechanical | Risk | Size |
|---|---|---|---|---|---|---|
| R1 **DONE 0.0.293** | Extract a `finish_toplevel(win, *, title=None, minsize=None)` helper (`gui/theme.py`) absorbing the `title/minsize/skin_toplevel/THEME_TOPLEVELS.append` clone | Violation 1 — confirmed literal copy | `gui/theme.py` (new fn) + `gui/doc_window.py:65-68`, `gui/restore_windows.py:68-71,283-286`, `gui/select_window.py:96-99`, `gui/image_viewer.py:126-128` | yes | low — pure extraction, same tests green, clone_guard drops the group | S |
| R2 **DONE 0.0.294** | Extract `apply_typed_wh(entry_w, entry_h, on_apply)` for the aspect W/H handler | Violation 3 — real clone invisible to the guard's default threshold | `agent_panel.py:723`, `api_panel.py:471`, `tool_panels/geometry.py:281` + new helper | yes | low | S |
| R3 **DONE 0.0.295** | Extract `poll_worker_queue(widget, q, on_result, *, poll_ms, after_attr)` for the poll-loop family, replacing the 3 ratcheted clone groups | Violation 2 — sidesteps the documented "base class differs" objection since a free function needs no shared base | `api_panel.py`, `dialogs.py`, `model_picker.py`, `sheetgen_panel.py` + new helper; ratchet shrinks by 3 entries | no — needs care around each class's own queue/`after` id attribute | medium — touches 4 files' async plumbing; reverses a previously-documented "duplicated on purpose" call, so flag it for the owner's nod even though it changes no architecture | M |
| R4 **DONE 0.0.297** | Split `painter/driver.py` into 5 responsibility mixins (`DriverLifecycleMixin`, `DriverProtocolMixin`, `DriverWaitMixin`, `DriverRecoveryMixin`, `DriverClassifyMixin`) composed into `SiteDriver`, mirroring the proven `gui/app.py` pattern | STRUCTURE LAW wall; ratchet entry + owner pre-approval already on record (2026-07-30) | `painter/driver.py` + 5 new modules; `tests/test_driver.py` (also ratcheted) splits alongside it | no — judgment on shared-state boundaries | low-medium — 1853-line dedicated suite, and the `gui/app.py` mixin split already proved the pattern works in this codebase | L |
| R5 | Split `gui/app_jobs.py` into the site run loop, a new api-image job module, and a dispatch-table module — the exact 3-way split the ratchet text already names | STRUCTURE LAW wall; owner pre-approved 2026-07-30 | `gui/app_jobs.py` + 2 new modules + `gui/app.py`'s mixin list | no | medium — coverage is only INDIRECT (no dedicated test file); run the full `test_gui_*` suite + a manual `uv shot` pass | M |
| R6 **DONE 0.0.296 — owner chose SPLIT** | Split `gui/agent_panel.py`'s 5 UI-sketch sub-panel builders into a sibling module, OR declare the file irreducible — the ratchet text itself offers BOTH outcomes | STRUCTURE LAW wall; the choice is explicitly left open in the ratchet | `gui/agent_panel.py:537-720` + possible new module | yes-ish (moving self-contained methods) | low — `test_gui_agent_visibility.py` + 6 more `test_gui_*` files reference `AgentPanel` | S-M |
| — | Move `gui/app_build.py:883`'s hex-colour pair into `gui/theme.py` | minor config-in-code | `gui/app_build.py`, `gui/theme.py` | yes | low, low value — optional cleanup | S |

Order of value/risk: **R1, R2** (mechanical, S, zero architecture change) →
**R3** (mechanical-ish helper, M, touches async plumbing — get the owner's
nod since it reverses a documented decision) → **R6** (S-M, but the SPLIT vs.
IRREDUCIBLE choice needs the owner, per the ratchet's own open question) →
**R4, R5** (L/M, pre-approved architecture change, still best run as their
own dedicated session given size).

**Safe for a sub-agent alone under the REFACTOR tooth** (same tests green
before/after + clone guard, no owner input needed): **R1, R2**, and the
optional colour move. **R3** is mechanically safe but should surface its
result to the owner before merging (it reverses `gui/__about/api_panel.md`'s
stated rationale). **R4, R5** are pre-approved in scope but large (L/M) —
recommend a dedicated REFACTOR session per file rather than folding into a
general sweep. **R6 needs the owner first** — the ratchet leaves the
split-vs-irreducible decision open by name.

## Evidence

- `!` `python tests/run_guards.py` → `18 passed`, exit `0`, ~42s.
- `!` `python rules/tools/clone_guard.py Gadgets/PromptPainter --ratchet
  Gadgets/PromptPainter/tests/clone_ratchet.json --json` → `{"failing": [],
  "stale_ratchet_ids": [], "same_file_warnings": 0}`.
- `!` `python rules/tools/clone_guard.py Gadgets/PromptPainter
  --min-statements 6 --json` → 4 failing groups (3 ratcheted + Violation 3).
- `!` `python rules/tools/clone_guard.py Gadgets/PromptPainter/gui
  --min-statements 4 --json` → 5 failing groups (same 4 + one nested
  sub-clone of an already-counted pair), 2 same-file WARNs.
- `!` commit `a0c1f37` "0.0.290 The screen floor was information all along —
  1280x720 no longer fails the layout audit".
- `!` `git push origin main` → `485264e..a0c1f37 main -> main`.

Nothing skipped — this audit ran on the CODE (grep/read) for the whole
`gui/`/`painter/`/`main.py` source tree, not on impression.

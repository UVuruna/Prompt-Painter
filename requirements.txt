# Runtime — the CDP driver (the parser and --dry-run need nothing but stdlib)
playwright>=1.40

# Runtime — the background fix (painter/bg_remove.py) and review thumbnails
numpy>=1.26
scipy>=1.11
pillow>=10

# Runtime — the GUI theme (darkly base + modern ttk widgets) and the
# rounded controls layered over it (the same mix RHMH runs)
ttkbootstrap>=1.10
customtkinter>=5.2

# Runtime — GUI icon rasterization: QSvgRenderer for the .svg icons
# (the same SVG engine the monorepo build pipeline uses)
PySide6>=6.5

# Development — the offline test suite
pytest>=8

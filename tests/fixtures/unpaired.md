# Fixture — Unpaired Heading

**The Orphan — no prompt follows (test, 01h)** → `fixture/Orphan.png`

*(an italic note, then the next entry begins with no fence between)*

**The Paired — a prompt follows (test, 02h)** → `fixture/Paired.png`

```
the paired prompt, byte-identical
```

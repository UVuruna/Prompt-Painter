# Fixture — Legacy Entry Forms

## Section one

### Sun — Ancient of Days (`ancient_of_days.png`)

A prose note between the heading and its prompt is allowed.

```
heading-form prompt
```

**Moon** — `moon.png`

```
bold-token prompt
```

**Drop dirs** (pointers, not entries): dual → `colored` and the
stems live at `assets/weekday/x/` — this paragraph must stay prose.

## 2. SIGN look (`assets/zodiac/astrology/sign/`) — the dir section

**Aries**

```
bare-bold prompt
```
